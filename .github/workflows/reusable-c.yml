name: reusable build
on:
  workflow_call:
    inputs:
      test:
        required: true
        type: string
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: print env
        run: echo "building ${{ inputs.envi}}"